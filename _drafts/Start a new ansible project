```
1. Create ansible folder
ansible 
- inventories
    inventoryName
      all.yml
- vars
    vaults.yml
- main.yml

2. Create vault file 
ansible-vault create vaults.yml 

3. Add main.yml
- hosts: all
  vars_files:
    - vars/dags.yml
  tasks:
    - name: Push dags to remote 
      taskName:
```
